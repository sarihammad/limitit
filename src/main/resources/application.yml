spring:
  datasource:
    url: jdbc:mysql://limitit-db:3306/limititdb
    username: limitituser
    password: limititpass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
